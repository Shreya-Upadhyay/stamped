{
  "name": "@stamped/shared",
  "version": "0.0.0",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "tune": "vitest run cluster-report --disable-console-intercept"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
